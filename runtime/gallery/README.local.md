# runtime gallery

- 이 디렉터리는 얼굴 갤러리 로컬 자산 전용이다.
- 사람별 폴더를 만들고 얼굴 이미지를 넣는다.
- 폴더 이름은 `한글이름, EnglishName` 형식을 권장한다.
- 예시:
  - `홍길동, GilDong/`
  - `김철수, ChulSu/`
- 한 장의 이미지에는 얼굴이 1개만 있는 구성이 가장 안전하다.
- 이 디렉터리 안의 실제 사용자 이미지는 git으로 추적하지 않는다.
